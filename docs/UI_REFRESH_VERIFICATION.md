# 本机界面更新验证

验证日期：2026-09-12。Chrome DevTools 使用独立浏览器上下文，未修改日常浏览器产品数据。

## 已验证

- C 端挂载 Chat UI 控制器，只有一个 composer。
- 所有功能卡片位于助手欢迎消息内；无 Chat 外部卡片区。
- 训练量表单在消息内展开；取消不计算；4 × 12 × 40 返回 1,920 kg。
- 回答等待期间显示停止按钮，用户下一条输入草稿不因回复到达丢失。
- 知识来源可展开，刷新恢复当前产品消息，新对话清空当前显示。
- 管理端六个页签正确切换，一次只显示一个配置面板。
- 编辑知识后添加条目不丢草稿；Workflow 上下移动生效。
- 保存卡片后保留 calculate_training_volume 能力，温度 0 保持为 0。
- 管理端保存内容可在 C 端刷新后读取。
- 停用入口和未知入口都禁止挂载对话界面。
- 375px 手机宽度下 C 端及管理端全部六页签均无页面横向溢出。
- 管理端桌面布局和 C 端手机截图已检查；浏览器无 error / warn。
- admin.js、public.js、chat-ui-kit.js 语法检查通过。

## 当前边界

仍为本地数据和知识摘录演示。未接真实模型、后端 Workflow 或第三方 Chat UI 框架；模型与 Workflow 表单保存配置，不宣称已执行。未上传服务器。
